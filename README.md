### [Task Master](https://thrills-task-master.herokuapp.com/)

A basic web app, that performs basic CRUD operations, allows users to:
- create tasks
- update tasks
- delete task

built using: python `flask`library and `sqlite` as database